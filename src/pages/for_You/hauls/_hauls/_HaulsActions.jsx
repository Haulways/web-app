import { Button, CreateButton, TopToolbar } from "react-admin";


export const HaulsCreateActions = () => (
    <TopToolbar>
        <CreateButton />
    </TopToolbar>
);

export const HaulsListActions = () => (
    <TopToolbar>
        <CreateButton />
    </TopToolbar>
);